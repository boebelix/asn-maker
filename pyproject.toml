[project]
name = "asn-maker"
version = "0.1.0"
description = "ASN label generator for 30x15mm label printers"
requires-python = ">=3.11"
dependencies = [
    "pillow>=12.2.0",
    "qrcode>=8.2",
    "reportlab>=4.5.1",
]

[project.scripts]
asn-maker = "asn_labels:main"
